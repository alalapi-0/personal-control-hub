# HUB-GOV-2026-09-06-TC18 · v1

Issued by read-only Governor `/root/tc16_governor`. Lane GOVERNED. This unit resolves UI scope for final Goal acceptance; it does not reopen TC16 management connections. Root owns writes, evidence/state, Git and external effects. Bounded agents may gather disjoint read-only evidence; no descendants.

## Objective and bindings

Produce one validated frozen scope-resolution record covering exactly the current 24 registry IDs, bound to the current registry hash, manifest-v5 ID/content hash, accepted TC16 outcomes, exact unit-02 inventory identity and current policy/owner references. The record supplements manifest-v5's historical startup dispositions and becomes the stage-A UI-scope acceptance evidence; it is not a new registry or source authority.

## Protected boundaries

Preserve registry, manifest-v5, authority-bundle-v4, all source bundles/plans/adapters, relations, ledger head130, all prior accepted records and TC17 code/Figma/store14/import/owner history. No manifest-v6, successor source bundle, project refresh, external implementation or final Goal completion in this unit. No business data, credential/config access, broad external scan, external launch/write, or Manga path resolution/probe/read. TC17 awaits the actual owner's separate design choice.

Reuse unit-02 discovery where sufficiently identified. If current identity or ambiguity prevents resolution, read only an exact cited source, through the registry root and a lexical allowlist with symlink-escape rejection. Gather only UI/CLI evidence and fingerprints; no full source copies in the delivered record. Runtime verification remains false for source-only evidence. No new external runtime verification is required.

## Record and acceptance

1. Exactly 24 unique current registry IDs, zero omissions/duplicates.
2. Every row has final ui_disposition, plain meaning/rationale, evidence/source identities, basis type, truthful runtime_verified, external_implementation_required:false, TC16 management-outcome reference and exact protection/recovery condition.
3. Allowed dispositions: in_scope (project-owned UI exists or belongs in design governance; no redesign mandate), no_ui (evidence establishes no project-owned product UI; management stays included), protected (applicable boundary/owner authority), deferred (only explicit current owner/policy authority plus exact recovery condition). No unresolved or silent promotion of proposals.
4. Resolve desktop-tool from exact evidence or ask the owner one bounded material question; do not silently relabel it no_ui. Finish other rows first if a decision is needed.
5. Separate UI scope, management outcomes and implementation authority. The representative TC17 runtime evidence cannot be generalized to other projects.
6. A repeatable validator verifies IDs, bindings, vocabulary, evidence references/counts and protected preimages; no external rereads during validation.
7. Final handoff will cite the accepted resolution while retaining historical manifest-v5 dispositions unchanged.
8. Freeze the exact evidence/state candidate; fresh Judge PASS then Governor approval before scoped commit, normal push on the existing branch and exact remote verification.

## Stop conditions

Unexpected source path/ownership/secret/business content, identity drift requiring broader access, unresolved protection, or a material owner decision stops only that affected row. Preserve completed evidence and continue independent rows. A failed check does not authorize source refresh, metadata rewriting or scope reduction.
