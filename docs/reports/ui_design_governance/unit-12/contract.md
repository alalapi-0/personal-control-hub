# HUB-GOV-2026-09-06-TC12 · v1 · ACTIVE

Read-only Governor `/root/tc4_governor` issued this contract. Root owns visible UI, STATE, evidence and Git. No numerical budget.

## Objective and authority

Restore the existing Figma connector OAuth connection, then separately verify official tool loading and `whoami`. The owner explicitly authorized reauthorization of Figma write access and now selected `@Figma` to verify and continue. The owner's earlier request to click the authorization personally remains in force.

## Authorized effect and boundaries

Use supported visible UI on official ChatGPT/Figma domains to reconnect only connector `connector_68df038e0ba48191908c8434991bbac2`. Continue the final authorization only for the same accounts and existing scopes, without new account, organization, workspace or sensitive-scope choices. Any owner-required login/consent step stays with the owner on the visible page.

Do not modify API Keys, Keychain, Codex configuration, other apps or permission modes. Do not read, copy, output or save session/cookie/token/authorization-code material. Do not restart/control Codex processes or bypass a rejection, browser protection or OAuth page. No Figma file/node/design calls or writes belong to TC12.

## Acceptance

1. Record only nonsecret preconditions: connector identity, installation/enabled state, local enabled flag, app permission mode and management-page connection state.
2. Reconnect only the existing Figma connector; preserve other apps, connections, configuration and credentials.
3. Verify official OAuth origin and the requested scope; stop for different/uncertain account or expanded scope. Hand off passwords, passkeys, 2FA and personal consent.
4. After completion, verify the management page's connection state as UI evidence only. Do not claim hot reload.
5. In a subsequent fresh runtime, verify actual Figma tool availability and official `whoami` success against the expected account. Only both passing prove runtime OAuth restoration. Neither proves a design write has succeeded.
6. Save only redacted results, official domains and times. No sensitive screenshot identity or authentication material in Hub evidence.
7. Root freezes the exact Hub candidate; fresh Judge PASS and Governor decision precede scoped normal commit/push and exact remote SHA verification. OAuth/global configuration never enters Git.

## Stop and recovery

Stop the affected action for UI/security/OAuth denial, a nonofficial destination, owner-only authentication, different/uncertain account, new scope/team/workspace, requested disconnection/configuration change, or uncertain outcome. Keep the visible page or precise recovery point. Check uncertain outcomes before retrying; never blindly resubmit OAuth. Do not disconnect or reset enabled=true as a rollback. Runtime and UI failures stay distinct. Keep the full Hub objective unfinished until its own acceptance is met.
