# TC8 · Three owner-authorized connection exceptions

The owner reply “批准例外” resolves only desktop-downloads-scripts, desktop-magnet and pycharm-misc-project. Canonical declarations are in registry hub_connection_exception and exact provenance in owner-decision.json. All 24 projects remain; permissions and business fields are unchanged. Manga remains pending; Figma authentication is unresolved. This is not UI or Goal completion.

Manifest v4 and source plan/bundle/relations v3 bind the updated registry. Older authority versions and all108 ledger events remain byte-identical and readable. Registry rebinding makes historical observations report authority drift until a later authorized refresh; no new business observation is asserted here. Three connection records preserve unknown/source_not_declared, exit2 and UI UNVERIFIED while recording AUTHORIZED_EXCEPTION. API declared.hub_connection_exception exposes the approval separately from business and operational facts.

Root verified185 Hub tests,11 control-plane tests, repository/registry/round checks,3 authority versions and7 connection records. Reproduce the no-source generation, exact preservation and24-project HTTP list/detail checks with:

```sh
python3 docs/reports/ui_design_governance/unit-08/verify.py
python3 scripts/hub_refresh.py validate
python3 scripts/hub_connections.py validate data/design_governance/manifest-v4.json data/design_governance/connection-exceptions-v1.json
```

No external project/source reads, Manga probes, business processes, ledger writes or Figma effects were needed. Global dirty work remains protected. candidate-v1.json identifies reviewed semantics; review/decision/delivery metadata is separate. Continue only the remaining owner scope and Figma dependencies after this unit's delivery.
