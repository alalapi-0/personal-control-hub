# HUB-GOV-2026-09-06-TC8 · version 1 · ACTIVE

Governor /root/tc4_governor supplied this contract. Root records GOVERNED ownership. Reader: Root, fresh Judge and Governor. Update only frozen objective, boundaries, effects, acceptance or budget. No numeric budget.

## Objective and boundaries

Record the owner's exact “批准例外” reply for desktop-downloads-scripts, desktop-magnet and pycharm-misc-project as AUTHORIZED_EXCEPTION. Registry project.hub_connection_exception is canonical; unit-07/dependencies.json is a derived dependency index. Preserve unknown business/source facts and all 24 members. Manga remains pending unless a separate explicit decision changes a revised/successor contract.

Root owns registry, successor authority, STATE, project code, evidence and Git. No project enablement, access profiles, allowed entries, source paths, scanning, business fields or membership change. Preserve every old authority version and refresh record. No project probes/source reads/external writes, business processes, Figma/UI work, invented design decision or final Goal acceptance. Protect unrelated global dirty files. Root may update only the dependent project_registry_sha256_current binding in storage program STATE, preserving every execution field.

## Authorized effects

Add exactly three registry declarations tied to owner-decision.json. Produce successor manifest v4, source plan/bundle/relations v3 with existing schemas; preserve historical versions. Record three existing-schema connection evidence items through the no-source path, and expose the declared exception separately in the existing DTO. Update compact STATE and dependency index. After fresh Judge PASS and Governor APPROVE, make one scoped commit, normally push the current tracking branch, and verify its exact remote commit.

## Acceptance

1. Exactly three exceptions match the owner's exact quote, stable turn reference and reason.
2. All prior permission/source/business fields and Manga/other projects remain unchanged.
3. New authority matches registry; all historical authority and ledger records remain intact and readable.
4. Each AUTHORIZED_EXCEPTION record has matching authority, verified no-source snapshot, exit 2 and UI UNVERIFIED; no false connected/UI claim.
5. Zero project probes, source reads, external writes/business processes, Manga probes or ledger mutation.
6. List/detail API exposes exception separately from unknown business state; no CONNECTED_AND_VERIFIED inference.
7. Three source-absence dependencies resolved; Figma and Manga remain explicit; sole STATE accurately points to next work.
8. Schema, authority, registry, bundle, records, API, repo, round consistency, zero-probe and preservation checks pass.
9. Exact semantic candidate/preimages receive fresh independent Judge PASS and Governor APPROVE before Git delivery.

## Recovery

Use exact registered preimages or targeted forward repair, never reset history, broad-stage or overwrite concurrent work. Preserve a local accepted commit on push failure; inspect remote before retry. PASS/decision/next-action metadata is outside semantic identity. All remaining UI/design/final handoff requirements continue unchanged.
