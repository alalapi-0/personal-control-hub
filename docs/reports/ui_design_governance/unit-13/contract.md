# HUB-GOV-2026-09-06-TC13 · v1 · ACTIVE

Read-only Governor `/root/tc12_governor` issued this contract. Activation prerequisite met: TC12 Judge PASS / Governor APPROVE, commit and exact remote `8820f001cba8c8cf0d4f00a0fbae7e37ea7cd85a` verified. No numerical budget.

## Objective and authority

Deliver three editable complete Hub directions: 清晰编辑台、专注画布、柔和工作室. Each uses the same explicitly synthetic/mock fixture and covers project overview, project detail, design list/comparison and necessary state designs at desktop 1440×900 and mobile 390×844. Deliver corresponding local exports and exact Figma node content identities for owner comparison. The owner already authorized these candidates; do not ask again or select for the owner. No Hub UI implementation in this unit.

## Protected boundaries and authorized effects

Root alone owns external Figma effects, Hub control plane, evidence and Git. Local writes only `STATE.yaml` and `docs/reports/ui_design_governance/unit-13/` (including necessary previews). Preserve all pre-existing dirty files, design store/ledger/registry, application code and external project contents. Do not read Manga or business data. Do not change OAuth, credentials, accounts, permission modes, connector configuration or plans. Never persist account identity in Git.

Use TC12's verified account and existing single plan to create one new Figma design draft; create/edit/read/export only its task-owned pages, frames, minimal components and variables. No sharing, publication, movement or overwrite of existing Figma files. Task-owned nodes and failed intermediates may be coherently replaced, merged or recoverably removed in that one draft; no extra design-system product, compatibility shells or backups.

## Acceptance

1. Three named, editable, complete directions with meaningful typography, density and component-language differences beyond palette.
2. Every direction covers overview, detail, design list/comparison and state designs in both specified viewports, using identical fixture content and comparable states.
3. UI and evidence identify example data; source, status and refresh/error meanings remain truthful. Ordinary screens omit meaningless engineering fields.
4. Cover required loading/empty/error/disabled states, mobile long text, 200% text layout, contrast and visible focus; never convey information by color, position or motion alone.
5. Mobile preserves key content, controls, task meaning and priority without unreadable truncation.
6. Record file key/URL, exact page/node IDs and frame sizes, node content version/hash, export paths/hashes. Read-back and exports prove correspondence to final nodes.
7. Separately report editable nodes, visual/desktop/mobile/accessibility design, fixture isolation and export correspondence; real console/network may be NOT_APPLICABLE with design-stage reason. Runtime interaction, persistence, effects and implemented behavior remain UNVERIFIED until implementation, not falsely passed.
8. Present same-order comparison, tradeoffs and exact versions to owner; preserve three candidates. Owner choice remains pending.
9. Root freezes Hub candidate and Figma content identity, runs first-write gate, structure/hash, state consistency and relevant governance checks; fresh Judge PASS and Governor APPROVE precede scoped commit/normal push and exact remote SHA verification.
10. TC13 acceptance means candidates ready, not selected, implemented or overall Goal complete.

## Recovery

Stop the affected effect for a different/uncertain account, multiple plan/team/workspace choice, broader permission, wrong file, sensitive auth material, uncertain write outcome or unmatchable exports/nodes. Save the precise recovery point and inspect remote state before retry; never duplicate-create blindly. Repair only task-owned content, leaving all other files/connections/settings intact.
