# HUB-GOV-2026-09-05-TC5 · version 1 · ACTIVE

Reader: Root, bounded Repair, fresh Judge and Governor. Purpose: reconcile the self-contained predecessor baseline before TC4 Git delivery. Current work remains in STATE.yaml. Update only for a frozen objective, boundary, effect, acceptance or budget change. No numeric task budget is specified. Governor /root/tc4_governor activated this successor after exact TC4 approval; owner authority is the 2026-09-05 Goal resumption and execution specification section 11.

## Objective

Reconcile and independently accept the smallest self-contained Hub predecessor baseline required to install and deliver the approved TC4 service: current registry authority, accepted storage parameters/adapter, required accepted TC1–TC3 source and support records, and affected legacy control tests.

## Boundaries

- Preserve frozen TC4 implementation/protocol/evidence and accepted TC1–TC3 facts. Current-work transitions and delivery/review metadata do not rewrite historical acceptance.
- No external management-authority cutover in TC5; external storage remains read-only. Preserve established project/storage facts and keep storage effects disabled.
- Preserve global-governance and unrelated dirty files. No Manga probes, Figma workaround, business-project writes, broad staging, duplicated authority, main merge, force push, publication, deployment, remote changes or secrets.
- Root is sole contract, control-plane, evidence, STATE, and Git writer. Bounded Repair may change only explicitly assigned Hub implementation/tests, may not spawn descendants, write external sources, self-review or perform Git effects.
- File placement, test replacement, refactoring and cleanup remain adaptive. Existing acceptance and attempts remain cumulative.

## Acceptance

1. Every dependency required by TC4 has an exact accepted identity or fresh authority-backed validation.
2. Registry content is bound to current external-state/root-migration evidence, not inferred from paths or dirty state.
3. Storage parameters and adapter retain accepted SG-GOV-2026-09-03-TC2 semantics.
4. Four affected legacy control failures are repaired to current authority semantics or obsolete assertions replaced/removed; no scope-owned failure is relabeled unrelated.
5. The baseline is self-contained, reproducible from Git, and passes relevant repository, registry, storage-control, state-consistency and TC1–TC4 dependency checks.
6. Exact candidate, preimages, provenance, preservation and reproducible checks receive fresh Judge PASS and Governor APPROVE.

## Delivery and recovery

After approval, Root reviews/stages the exact task-owned TC5 unit and sensitive-content check, commits once, normally pushes the verified current tracking branch, and verifies the exact remote commit. Expected remote origin, branch agent/governance-closure-20260812, upstream origin/agent/governance-closure-20260812; reverify before effects. Then separately deliver unchanged approved TC4 and verify its remote commit. Do not combine unrelated dirty files or use broad staging. Preserve a local commit after push failure; inspect an uncertain remote outcome before retrying. No history rewrite, stash/reset or whole-repository rollback. Use exact task-owned preimages after checking concurrent edits.

Management-material consolidation and external authority cutover follow under TC6. That successor may version authority/default-bundle behavior after TC4 delivery, preserving accepted service guarantees while establishing one management authority and one storage execution state. TC5 does not accept UI, design choice, final connections or the overall Goal.
