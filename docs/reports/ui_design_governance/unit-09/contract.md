# HUB-GOV-2026-09-06-TC9 · version 1 · ACTIVE

Governor /root/tc4_governor supplied this contract; Root records GOVERNED ownership. Reader: Root, fresh Judge and Governor. Update only a frozen dimension. No numerical budget.

## Objective and authority

Record exactly manga-localizer as an owner-approved visible management exception through the accepted TC8 mechanism. Resolve only its owner-scope dependency while keeping zero project access. Exact owner instruction: “在 Codex 中重新授权 Figma 插件写入权限。批准可见接入例外。” Thread 01a071cc-f2c6-76c2-bc43-f300db610b0b, turn 01a07400-d059-7f90-a685-2e3250945f2d, message 01a07400-d0c6-7803-af77-7a9cb5bd7caf. TC8 commit 9795e37d30dc3860e7bbf05cfc4c47b9ab35fd8f stays accepted and closed.

## Frozen boundaries and effects

Preserve all 24 members, all prior three exception declarations, all historical authority/exception/refresh records and 108 ledger events. Preserve Manga disabled status, access profile, scan/profile/inventory/inspection/validation/mutation flags, empty source declarations, business fields and storage restrictions. Rejection precedes path resolution: zero project probes, reads, process launches or writes.

Root exclusively owns registry, successor manifest/source-plan/bundle/relation/exception records, default bindings and necessary fixture adaptation, dependent storage registry-hash binding, dependency index, Hub STATE, evidence and Git. Reuse existing schemas and mechanisms; no new mechanism. Storage STATE changes only project_registry_sha256_current. Protect unrelated dirty files and the single uncommitted Figma observation artifact containing four observations; it remains future design input outside TC9 evidence/candidate/Git. Preserve prior TC8 delivery metadata locally.

Figma write authorization is valid for later connected design work, but official whoami remains UNAUTHORIZED / oauth_token_invalid_grant. TC9 makes no Figma/CUA call, workaround, upload, design edit or connection-success claim. No external project modification, UI work, design-store decision, final connection/UI acceptance, main merge, force push, publication or deployment.

After fresh Judge PASS and Governor APPROVE, Root may create one scoped commit, normally push the verified current tracking branch and confirm the exact remote commit.

## Acceptance

1. Exactly one new Manga AUTHORIZED_EXCEPTION, with exact owner quote and stable turn/message reference.
2. All original registry fields, Manga restrictions and the three prior declarations are preserved.
3. Successor authority matches registry; predecessor versions and ledger remain byte-identical/readable.
4. Manga evidence matches its authority-blocked snapshot, exit 2, UI UNVERIFIED and owner reference; no connected/source-read/business-state claim.
5. List/detail exposes the declaration separately from unknown business state with parity for all 24 IDs.
6. Instrumented checks prove zero Manga resolution/probes/reads/launches/writes and zero external effects.
7. Manga owner-scope dependency is resolved in derived index and STATE; Figma remains the sole explicit blocker and OAuth remains unauthorized.
8. Storage execution fields are unchanged; only current registry hash follows the new registry.
9. Relevant schema/authority/registry/records/API/repository/round/state/ledger/protected-diff checks pass.
10. Exact semantic candidate/preimages receive fresh Judge PASS and Governor APPROVE before delivery.

## Recovery

Targeted forward repair or exact task-owned preimages only; no resets, broad staging or historical deletion. Exclude locks, global dirt and Figma observation input. Preserve a verified local commit on push failure and inspect remote before retry. PASS/decision/next-action/delivery metadata is outside semantic identity. TC9 is not Figma connectivity, design choice, UI or overall Goal acceptance.
