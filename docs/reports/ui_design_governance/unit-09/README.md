# Manga visible management exception

Reader: owner, Root and independent reviewers. Purpose: TC9 evidence index; update on material candidate change. Current execution remains solely in STATE.yaml.

The owner explicitly approved Manga's visible management exception. Only its registry declaration is added. Manga remains disabled/no_current_goal_access with zero path probes or reads. Its authority-blocked, unknown snapshot has AUTHORIZED_EXCEPTION evidence with exit 2 and UI UNVERIFIED; the declaration is separate from business state in list/detail DTOs.

Manifest v5, source plan/bundle/relations v4 and exception projection v2 preserve all previous versions and the three TC8 declarations. The ledger stays byte-identical at 108 events/98 results/5 requests. Storage execution changes only its dependent registry hash. The Manga owner-scope dependency is resolved; Figma OAuth remains unauthorized despite the owner's write authorization.

contract.md and owner-decision.json define exact authority. baseline.json records Git preimages, protected/history hashes and ownership. verify.py reuses the accepted HTTP readback and enforces exact field preservation and no-source/no-access guards. validation.json records 185 Hub +11 control tests and all relevant registry/authority/repository/round checks. One fixture count assertion was corrected for the fourth bundle; the final full suite passes.

Candidate/Judge/Governor records govern acceptance and delivery. TC9 does not prove runtime UI, Figma connectivity, a user design selection or full Goal acceptance. The uncommitted Figma observation file and unrelated global edits remain outside this delivery.
