# HUB-GOV-2026-09-05-TC6 · version 1 · ACTIVE

Reader: Root, bounded Repair, fresh Judge and Governor. Purpose: the owner-authorized management-material consolidation and single-authority cutover. Current Hub work remains in STATE.yaml. Update only for frozen objective/boundary/effect/acceptance/budget changes. No numeric task budget is specified. Governor /root/tc4_governor supplied this successor, activated after verified TC5 commit 4c2852cdf9e20fbcb88abd83fd8d1b7d26ec2e1c and TC4 commit a5e72b977d054743c79e25a0b3cbbc2f58d84c76 on the current tracking branch.

## Objective

Consolidate Hub-owned storage-governance management authority into one coherent Hub governance-program namespace, with one active versioned authority bundle and one storage execution state. Switch every verified consumer to that authority while preserving accepted ledger/history, immutable historical bundles and the established closed, zero-effect storage outcome. The router, governing specification and execution state remain one named-source group; expected namespace governance/programs/storage_governance/, with exact implementation paths adaptive.

## Boundaries

- STATE.yaml is the sole Hub current-state authority, storing compact Hub management facts and pointers; it must not duplicate storage execution state.
- Preserve one storage execution authority, the full accepted ledger/history, registry identity, completed outcomes and closed project-effect authority. Normalize stale paused/active fields only to the evidenced terminal closed condition; invent no execution history and resume no work.
- Move only the Hub historical repository-inventory paragraph from ~/PycharmProjects/.agent/STATE.md. Byte-preserve independent global-governance and other workspace sections.
- Retain ~/.config/storage-governance/guard.sh and required host configuration in place and unchanged.
- No Manga probes, Figma workaround, business-project writes, storage migration/cleanup, credential/config edits, deployment, publication, main merge, force push or unrelated staging.
- Root exclusively writes control planes, external management sources, STATE, evidence, cleanup and Git. Bounded Repair only writes its assigned disjoint Hub implementation/tests; no children, self-review, external or Git effects. Implementation/refactoring/tests and superseded task-owned routes remain adaptive.

## Authorized effects

After exact native writer verification, preimage registration and consumer discovery, Root may move/merge the three management sources into the Hub authority group, update exact Hub/external management entrypoints, and recoverably remove superseded editable copies after consumer verification. A demonstrated host consumer may retain a minimal forwarding entry that resolves to the same single authority.

Version the authority/default list used by connection management and project-service defaults, including connection_manager_cli.py, connections.freeze_manifest and project_service.py semantics. This does not authorize storage/business execution. Existing user authority and execution specification sections 10–11 govern the exact local management cutover and subsequent scoped Hub Git delivery.

## Acceptance

1. One program-readable consolidation inventory records bounded sources, ownership, purpose, readers/writers, versions, targets, dispositions, validation and unresolved reasons; it is evidence, not another current-state authority.
2. Router, specification and execution state are co-located as one Hub-owned authority group with one versioned identity and exactly one editable storage execution state; no mirror, dual write, competing truth or stale authoritative consumer remains.
3. All verified consumers use the intended versioned authority/default list. Accepted refresh facts and ledger history remain readable without silent migration/reset.
4. Storage state consistently reports the established completed, closed condition with zero active writers/projects/pending effects or renewed effect authority.
5. Hub STATE retains only its compact management summary and authoritative pointer.
6. The Hub historical paragraph is consolidated; all independent global/workspace bytes are preserved. Host guard/configuration stay unchanged and operational; other retained materials have a verified dependency or protected ownership basis.
7. Consumer readback, rebuild, registry, adapter, state-consistency, service, interruption/restart and repository checks pass, including 24 project projections with Manga blocked and unprobed.
8. Exact Hub/external preimages, authority mapping, semantic candidate, preservation and reproducible checks receive fresh Judge PASS and Governor APPROVE.
9. After approval, Root reviews/stages only this unit, commits once, normally pushes the verified current tracking branch and verifies the exact remote commit. Record external cutover evidence with the same candidate, without a competing progress ledger.

## Recovery

Use exact registered preimages and forward repair. Remove an old source only after consumers pass against its replacement and concurrent-change checks succeed. Restore only the exact affected group whose preimages remain valid; never reset shared Git or unrelated workspace state. Preserve a local commit after push failure; inspect uncertain remote state before retry. No overall Goal, UI/Figma choice or final connection acceptance is implied.
