# HUB-GOV-2026-09-05-TC1 · version 1

Reader: Root, fresh Judge, Governor. Purpose: frozen capability contract for the first Hub foundation unit. Update: explicit boundary/acceptance amendment only. Created by read-only Governor `/root/hub_contract`; registered by Root. This is a historical contract, not a second current-state authority.

## Objective

Establish the Hub Goal in `STATE.current_work`, freeze all 24 registry identities, and deliver versioned, machine-validated scope/connection-manifest/snapshot/evidence records with bounded read-only connection infrastructure. This unit does not claim the Hub product, all connections, or UI acceptance complete. Implementation paths, framework and test organization remain adaptive.

## Protected boundaries

- Preserve all initial dirty content, recorded in `baseline.json`; only the authorized `STATE.current_work` handover and its timestamp may change in that initial set. Keep storage completion facts, pointers and historical review information intact. `AGENTS.md` + `STATE.yaml` must remain at most 8192 bytes.
- Registry is the only editable project list. The Hub connection manifest references its IDs and hash; it is distinct from both existing storage manifests, whose differing pointers remain unchanged.
- All external projects, business state/data/code/services and concurrent tasks remain read-only. No secrets, datasets, media, prohibited directories or real `.env` reads.
- Manga Localizer remains `no_current_goal_access` pending an explicit owner response. Do not even probe its filesystem. Record `BLOCKED_BY_AUTHORITY`, not an agent-approved exception.
- No selected UI implementation before the owner selects a Figma candidate. This unit contains no product UI, user selection, Figma writes or external business changes.
- Only Root writes control-plane/state/evidence and initiates external effects. Any assigned Repair must have bounded disjoint non-control-plane ownership and cannot spawn or perform Git/external effects.

## Authorized effects

Read registry-authorized rule/state/supporting files and necessary read-only Git identity. No external project writes, real API calls, login, deployment, publication, merge, commit, push or remote changes. Figma OAuth returned `UNAUTHORIZED / oauth_token_invalid_grant`; design creation is pending reconnection, not waived.

## Acceptance

1. The sole STATE records the Goal, true unit/status/blockers and exactly one next action; no premature completion or acceptance. Bootstrap stays within 8192 bytes.
2. Manifest binds registry version/hash and all 24 IDs with zero omissions/duplicates and no copied project-root list.
3. Every entry expresses permission basis, allowed-entry references, expected capabilities, UI disposition, source absence and evidence references. Disabled/no-access, bounded-self and no-state records remain distinct.
4. Versioned scope/manifest/snapshot/connection-evidence validators enforce IDs, references, enums, timezone timestamps, fingerprints, unknown reasons, versions and permissions.
5. Reads expand paths/symlinks, remain inside the authorized root and named entry, enforce bounded size, safely parse structured content and never execute source instructions/hooks/tags.
6. At least one real authorized source smoke proof; isolated tests cover missing/corrupt sources, unknown versions, out-of-root and symlink escapes. Do not label this full coverage.
7. Preserve the three no-current-state projects and continue investigating their allowed entries; never fabricate business status from source absence.
8. Candidate checks pass; precise pre-existing storage validation failures may remain, but no regression, scope expansion or loss of state facts is accepted.

## Evidence and review

Register HEAD, dirty preimage hashes, exact candidate hashes/diff, registry/manifest hashes, commands/exit codes, real source/time, negative tests, output file list and bootstrap bytes. Run prewrite runner/gate; run schema/coverage/read-only tests, check_registry, check_repo and round_consistency. Use available test runtime and explicitly report unavailable pytest separately.

Freeze the complete candidate once ready. A fresh read-only Judge evaluates this exact contract and candidate; Governor decides after Judge PASS. Any content change creates a new candidate identity. Restore only task-owned changes using the registered preimage/inverse diff; no broad reset/stash/backups. Contract creation is not candidate approval.
