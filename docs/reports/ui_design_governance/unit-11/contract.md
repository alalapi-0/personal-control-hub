# HUB-GOV-2026-09-06-TC11 · version 1 · ACTIVE

Read-only Governor /root/tc4_governor issued this contract. Root exclusively owns writes, evidence, STATE and Git. No numerical budget. Owner reported accidentally disabling Figma and asked to inspect/fix; prior explicit Figma access/write authority remains.

## Frozen scope

Restore only ~/.codex/config.toml apps.connector_68df038e0ba48191908c8434991bbac2.enabled from false to true. Capture fresh hash/mtime/mode and redacted structure before mutation; TC10’s older hashes are historical. Preserve every other byte and field: models, reasoning, approvals, permissions, other apps/MCPs, authentication, plugin installation/user-enabled state, OAuth, sessions, Keychain, browser and runtime configuration. No reinstall, removal, reconnect, update, process restart/control, permission or credential change. No Figma remote call in this unit.

## Acceptance

1. Fresh preimage proves this target exists and its sole field is enabled=false. Official config semantics and local evidence identify it as a connector-loading disable, while backend plugin remains installed/user-enabled and app permission allows actions.
2. Postimage TOML parses; target is exactly true. Reverse only this token and reproduce the original full-file hash; all other bytes/fields and mode remain unchanged. Compare current bytes to the preimage immediately before writing; stop on concurrent change.
3. No secret value, credential fingerprint, session or browser auth enters evidence/output/temp/Git. Global config never enters Git. Root preserves unrelated Hub/global dirty paths; existing accepted TC10 work stays closed.
4. Record current-turn missing Figma tools as a pre-change runtime observation. A written true value is not runtime/OAuth success. STATE distinguishes diagnosed local disable, completed restoration, and UNVERIFIED fresh-turn discovery/whoami.
5. Exact verification, repository/state checks, diff and scoped secret scans pass. Freeze exact Hub candidate, fresh Judge PASS then Governor APPROVE; normally commit/push only registered Hub scope and verify remote SHA.
6. Later fresh Goal turn may separately inspect tools and perform already-authorized whoami. No Figma file mutation, OAuth repair or complete-Hub claim here.

## Recovery

Never overwrite concurrent edits. Only syntax failure permits exact owned preimage restoration, and only if the file still matches the owned postimage. Runtime/OAuth failure does not authorize setting false again; retain true and report the separate blocker. Review/decision/delivery and STATE status/next-action/timestamp metadata do not reopen unchanged semantic evidence.
