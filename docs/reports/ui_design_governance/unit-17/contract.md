# HUB-GOV-2026-09-06-TC17 · v1

## v2 amendment (supersedes v1 before final freeze)

Issued by `/root/tc16_governor` after real deep-link testing exposed raw project IDs. Root may modify only `src/hub/web/hub.js` to reuse its project-name directory cache for design-list, project-design and comparison routes; direct/reloaded cache misses may perform the existing read-only `/api/projects` GET. Other product/test files remain excluded. Preserve layout/CSS/C-P5, refresh and design semantics, store revision14/imported facts, historical event, ledger130 and all other protected files.

Added acceptance: fresh/reloaded `#designs`, `#designs/computer-study-plan`, and its comparison show readable names; directory failure exposes a safe unavailable/retry view, not raw IDs; bounded GET only, no mutation/refresh/external requests; project/relation routes remain correct. Verify direct loads, reload/back, desktop/mobile, keyboard/focus and console/network before final freeze. All v1 objective/source/Figma/actual-owner/export criteria remain in effect.

Issued by `/root/tc16_governor`; ACTIVE after TC16 accepted/delivered. Lane GOVERNED. No numeric budget; ongoing Goal cumulative budget applies.

## Objective

Complete the representative stage-D path for `computer-study-plan`: truthful existing-UI baseline from authorized code; one editable Figma baseline and one materially distinct editable candidate with identical synthetic content/state/viewports; exactly one baseline/candidate import into Hub; actual owner comparison/selection and optional feedback; restart persistence and exact sanitized export. No external implementation and no final Goal handoff in this unit.

## Ownership and effects

Root owns canonical data, STATE, evidence registration, external effects and Git. Bounded writers may own disjoint Hub fixture/import/validation helpers, preserving others' work; no descendants. Only Root appends design store/owner decisions or writes control planes.

Authorized external source root is resolved only from current registry `computer-study-plan`. Copy byte-for-byte only `progress.html`, `progress_ui.css`, `progress_ui.js` to a disposable Hub-owned fixture. Protocol-only references are exactly `tests/ui/fixture_server.py` and `tests/ui/README.md` (Governor correction); never copy them to export materials. Source copies remain disposable local files and are excluded from Git/export; only helpers, identities and sanitized results are delivered.

Create/use one task-dedicated Figma file/page containing sanitized editable baseline/candidate. After fresh Judge PASS and Governor APPROVE, scoped commit, normal push existing tracking branch, exact remote verification.

## Protected boundaries

Preserve accepted Hub C/P5 v3 layout, palette, components, CSS and behavior; ledger head130 and all history (no refresh); existing design-store revision8 facts/history and one historical owner event through append-only additions; registry/adapters/bundles/manifests/plans/exceptions/relations/prior accepted evidence/unrelated work; external project byte-for-byte.

No source reads outside the five named files. Do not read real `progress.json`, `progress_data.js`, `rounds_data.js`, records/databases/private business content/secrets/Manga paths. Reject symlink escape. No external process launch/write/live API/business mutation, existing Figma asset modification, raw repository upload, account/config/storage/deployment/publication/main-merge/force-push/history rewrite.

## Required behavior and acceptance

1. Preflight: TC16 acceptance/exact remote delivery, ledgerhead130, design-store8/single historical choice, protected style identities, exact source allowlist.
2. Source-copy hashes match external originals. Instrumented copy permits only three source reads, zero other source reads/writes/launches. Earlier bounded protocol discovery is identified separately.
3. Loopback fixture serves exact allowlisted UI sources and task-owned synthetic GETs; writes/unlisted paths fail closed. Trust label identifies real UI code with synthetic example data. No unexpected outbound requests/private content.
4. Baseline records source identity/file fingerprints, observed route/tasks/actions/shortcuts/inputs/defaults/validation/states/GETs/persistence/side effects/viewports/unverified items. Observe primary path, relevant loading/empty/error, keyboard/focus/responsive/console/network; real code versus synthetic data stays explicit.
5. Figma baseline/candidate editable, exact file/page/node/version identities/local preview hashes/source bindings; same sanitized data/state/viewports. Preserve original recognizable baseline. Candidate meaningfully changes layout/density/hierarchy/component language, not palette only, without changing behavior/fields. Existing Figma assets unchanged.
6. Append exactly one baseline and one candidate plus their artifact references through accepted store mechanism, stable IDs/revisions/content hashes/scope/provenance/differences. Exact retry idempotent; old facts/choice intact.
7. Real Hub displays original/candidate comparison, source classification, differences/scope/Figma/offline previews/feedback/history. Ordinary UI omits raw engineering fields; diagnostic details remain available.
8. Owner makes actual decision against exact current candidate. No silence/recommendation/default/Agent/fixture choice may be recorded as owner decision. If changes requested, retain prior revision and iterate to actual selection; source drift requires stale/rebaseline handling.
9. Restart restores exact comparison/decision. Export bound to selected baseline/candidate and contains only sanitized previews, Figma refs, versions/scope/differences/decision/supplied feedback/hashes. Reject unsafe/missing/stale/mismatched artifacts.
10. Shared-family and independent branches may reuse accepted unchanged isolated mock evidence; real decision applies only recorded project/page scope, no automatic family propagation.
11. Complete UI rubric: functional/visual/desktop/mobile as supported/keyboard/focus/accessibility/reduced-motion/console/network/isolation/Figma editability/Hub persistence/export. Required UNVERIFIED blocks acceptance.
12. Freeze exact code/data/state/evidence/Figma/import/decision/export candidate; fresh Judge PASS before Governor approval and Git delivery.
13. Stage-E handoff/final reconciliation/overall Goal remain after TC17.

## Recovery

On source drift/allowlist escape/unexpected network or write/Figma identity mismatch/store conflict/stale candidate/uncertain effects/ownership overlap stop affected operation. Inspect exact Figma node or Hub request/event before retry; no fabricated replacement event. Preserve history and repair forward. With no owner selection leave TC17 active with exact candidate/comparison ready; continue independent work only.
