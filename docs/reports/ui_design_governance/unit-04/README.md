# Local Hub service evidence

Reader: Root, bounded Repair, independent reviewers and owner. Purpose: index TC4 capability and integration evidence. Update when candidate/evidence changes; current work and the unique next action remain in `STATE.yaml`.

- `contract.md`: TC4 v2 retains v1 capability acceptance and adds the owner's resumed, post-approval Hub Git delivery authority. TC3 remains accepted.
- `baseline.json`: 138 preimages, 137 protected files, accepted TC2/TC3 identities and sole-writer observation. Existing accepted implementations and historical facts are initially read-only; only STATE is an owned existing file.
- `state-task.diff`: canonical transition to the local service unit, preserving other state.
- `implementation-seams.md`: narrow read-only Explorer findings to reuse accepted APIs and avoid re-discovery.

The approved predecessor has 133 passing related tests and three real all-24 refreshes: 20 source-resolved, 3 verified absent current-state declarations and Manga authority-blocked. Its 72 results and 78 events are retained. Approval is for source-layer capability, not final connections or UI.

This unit implements no UI and creates no real owner choice. Figma connection and Manga scope remain owner dependencies; the local service can be built and tested with isolated fixtures while those dependencies remain unresolved. No external-project writes or publication. Only post-approval, task-scoped Hub commit and normal push are authorized under TC4 v2; unsynchronized predecessor ownership must be reconciled first.
