# HUB-GOV-2026-09-05-TC4 · version 2 · ACTIVE

Reader: Root, bounded Repair, fresh Judge and Governor. Purpose: capability contract for the local Hub application service. Update on owner objective/boundary/acceptance changes only. Execution state remains in STATE.yaml.

Governor /root/hub_governor_tc3 approved TC3 candidate v3 and conditionally activated version 1. Source: ../unit-03/governor-v1.json#successor, raw SHA-256 bd157b919ba35ee21abd0bc96a717b7eaed71c8c70649b616c52cdb372c0d3df; activation decision: ../unit-03/governor-v3.json. On 2026-09-05 the owner resumed the Goal and authorized accepted Hub units to commit and normally push the current tracking branch. Governor /root/tc4_governor issued REVISE_TASK for version 2: only the Git effect boundary and delivery closure change. The objective, 13 capability criteria, accepted TC2/TC3 identities, prior preimages and valid evidence remain cumulative. No numeric task budget is specified.

## Objective

Local single-user query/command service joining accepted source ledger, relations and design records/export; one application-facing contract, no UI acceptance.

## Boundaries

- Registry and manifests remain authoritative; no second project list or fact store.
- Preserve accepted facts and unknown/unavailable/stale/blocked/proposed meanings.
- No external-project writes, business execution, arbitrary discovery, Manga probes, publication, deployment, release, account changes or remote effects except the bounded Hub Git delivery below.
- No UI before Figma selection, no Figma while OAuth pending, no fabricated owner choice/review/authorization/implementation.
- Manual refresh inherits TC3 authority, bound, drift, idempotency and concurrency controls.
- Design events require exact explicit owner action; selection and implementation authority separate.
- Artifact delivery only by registered IDs, never caller paths.
- Loopback-only single-user service resistant to hostile webpage reads/writes; no LAN/public exposure.
- Root owns STATE/protocol/evidence/control; Repair disjoint source/tests only, no children/effects.

## Acceptance

1. One deterministic DTO derivation for list/detail; shared fields equal.
2. Preserve identity, raw/normalized state, next action, blockers, freshness, times, errors, relations, design state and provenance; missing remains unknown.
3. All24 IDs representable, Manga blocked zero probes, no promotion to final connection acceptance.
4. Deterministic search/filter/order/pagination if present and errors for identical snapshot.
5. Refresh only frozen registered IDs, idempotent identity, partial success and distinct authority/concurrency/validation errors.
6. Design actions select/request_changes/defer/withdraw bind request, expected revision, candidate ID/revision/hash, scope/source/supersession; no defaults or Agent choices.
7. Stale/invalid/schema/action shapes fail closed; conflict and duplicate handling; committed outcomes preserved across response failure and retry.
8. Artifact ID lookup verifies safe ordinary file, symlink boundary, scope/hash/classification/content behavior; no raw path, traversal or filesystem detail leaks.
9. Explicit tested trust model; reject non-loopback binds; Host/Origin/CORS and mutation protections; loopback alone is not owner intent.
10. Deterministic outcomes for invalid/not-found/authority/conflict/unavailable/precommit/postcommit/corruption; no secrets/business content/stack/unrestricted paths.
11. Isolated loopback start/query/stop lifecycle test with temporary fixtures, no external projects or business services.
12. Accepted CLI/library behavior stays on same facts/validation; rebuild works and unknown/corrupt records do not silently migrate.
13. STATE contains real dependencies and one next action; bootstrap and protected storage preserved.

## Evidence and recovery

Gate checks, DTO parity/coverage/provenance, refresh/decision/conflict/postcommit controls, artifact and hostile-origin tests, lifecycle/restart, schema/repo/state/bootstrap/preservation; exact candidate for fresh Judge and Governor.

Recovery uses task-owned preimages and inverse changes only, after checking concurrent edits. No reset, stash, backup tree, alteration of accepted historical facts, or unrelated writes. Frozen candidate requires fresh Judge then Governor. UI/owner choice/Figma/final connection/Goal acceptance remain separate; this activation grants no real owner design decision.

## Authorized Git delivery and closure

Authority: owner 2026-09-05 Goal resumption, STATE.yaml git_delivery, governance/agent_policy.yaml, data/gates/auto_advance_policy.yaml and execution specification section 11. After exact-candidate fresh Judge PASS and Governor APPROVE, Root may stage the exact verified task-owned TC4 unit and required control/evidence updates, create one scoped commit, normally push its current tracking branch, and confirm the remote contains that exact commit. Expected branch: agent/governance-closure-20260812; upstream: origin/agent/governance-closure-20260812; remote: origin. Reverify configuration, exact ownership, dependencies and absence of overlapping writers before Git mutation. Preserve unrelated/ambiguous dirty work, including global governance; no broad staging, remote changes, main merge, force push, history rewrite, secrets, deployment or publication.

Capability approval and Git delivery are separate facts. Delivery closes only after verified normal push. If dependency ownership or remote delivery is unresolved, retain the verified candidate or local commit and exact dependency; inspect remote state before retrying uncertain effects. Accepted predecessor files are not reopened or rewritten. Reconcile required unsynchronized Hub predecessors by exact prior acceptance and ownership evidence before delivering a self-contained unit; never silently collect protected baseline work.

Root records exact candidate hashes, accepted dependencies, protected preimages and preservation exceptions; reuses pause/TC2/TC3 evidence when relevant identities match, and reproduces material checks. Record reviewed staged scope, sensitive-content check, commit, push outcome and remote readback in existing evidence. Decision, delivery and next-action metadata are outside the semantic service-candidate identity; metadata alone does not reopen unchanged acceptance.
