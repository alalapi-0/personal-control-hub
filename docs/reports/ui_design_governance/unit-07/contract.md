# HUB-GOV-2026-09-05-TC7 · version 1 · ACTIVE

Reader: Root, fresh Judge and Governor. Purpose: final independent operational readiness using accepted production APIs. Update only for frozen objective, boundary, effect or acceptance changes. Governor /root/tc4_governor supplied this contract; activated after Root verified exact TC6 remote commit b0c25ef0d0ee0f2d56cb6cfe523b7a07a514c8a7. No numeric budget.

## Objective and boundaries

Refresh every permitted member of the frozen 24-project registry under accepted v2 authority; preserve ledger history/idempotency; read all24 through application API; initialize production DesignStore empty through its accepted initialize API; classify actual remaining dependencies.

Root alone owns production data, STATE, evidence, control-plane and Git writes. External reads use already authorized named-source adapters only. Manga rejects before path resolution with zero probes. No external writes, business launches, storage effects, Figma attempts, account/config changes, UI implementation, fabricated owner decisions, facts or candidates. Preserve unrelated global dirty files. Do not reset/replace/silently migrate existing stores. Do not add mechanisms; repair only a precisely reproduced new Hub defect, with minimal corresponding evidence and review. No final connection/UI/Goal acceptance is implied.

## Authorized effects

Root may run bounded source refresh through accepted adapters, append Hub ledger results, initialize the empty real design store, update compact current state/evidence, and after fresh Judge PASS and Governor APPROVE commit/normal-push the verified tracking branch and verify exact remote commit.

## Acceptance

1. Verify active v2 authority, frozen24 IDs, registry/adapters and ledger preimage/head before execution.
2. One request covers all24; every permitted source receives defined read-only handling; Manga is authority-blocked with zero probes/path resolution.
3. All prior records/accepted heads remain intact; valid continuation and same-request idempotency produce no duplicate effects.
4. API list/detail projections cover24 IDs at one consistent post-refresh head, preserving provenance, unknown/unavailable meanings, errors and relations.
5. Zero external write, business launch, storage/config effect, Figma call or real design/owner decision.
6. Production DesignStore.initialize creates a valid real empty store with zero facts/events; reopening is identical.
7. Classify remaining dependencies from evidence; Figma and Manga scope remain explicit, source-specific unavailable states remain distinct, no automatic final acceptance.
8. Repo, registry, authority, state-consistency, ledger, design-store, service/API, Manga-zero-probe and preservation checks pass.
9. Sole STATE records one-time first250k task-health rebaseline, this unit and one next action; no health ledger.
10. Exact semantic candidate/preimages receive fresh Judge PASS and Governor APPROVE, then one scoped commit/normalpush/exactremote verification.

## Evidence, recovery and stop

Record authority/store preimages, request/result identities, before/after heads, idempotent replay, API/empty-store readback, dependency classification, zero-effect counters, protected diffs and reproducible checks. On failure retain committed outcomes and repair forward; do not retry uncertain requests with new IDs. Restore only an uncommitted owned exact preimage still valid. Preserve a verified local commit on push failure and inspect remote before retry.

After this unit, stop readiness work at missing Figma connection and owner decisions; do not invent another readiness unit. Goal completion still requires the full execution specification.
