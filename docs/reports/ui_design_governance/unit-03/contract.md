# HUB-GOV-2026-09-05-TC3 · version 1 · ACTIVE

Reader: Root, bounded Repair, fresh Judge and Governor. Purpose: capability contract for durable project refresh and source resolution. Governor `/root/hub_contract` activated after TC2 candidate v3 approval; Root registered its decisions, preimage and sole writer. This document is not execution state.

## Objective and boundaries

Build persistent Hub refresh facts, current projections/history, last-success retention and evidence-backed relation proposals. Complete source-layer disposition for every one of the 24 registry IDs without claiming UI or final connection acceptance.

- Registry remains the sole project/root authority; preserve all 24 IDs and immutable prior manifest identities. No second editable project list.
- External projects remain read-only. Do not execute/import external scripts, hooks, schedulers or business code. No novels, media, databases, review content, credentials or other business data.
- Sources are exact registry-named entries, or bounded state files statically and explicitly named by allowed rule/diagnostic sources. No searches, globs, directory traversal or code execution to discover more sources. Resolve inside registry root; enforce ordinary-file, size, sensitive-path and symlink boundaries.
- For registry `watch_paths: ['.']` with no rule/current/supporting source, reuse only individual relative paths explicitly listed in the accepted TC2 `ui-source-inventory.json`. Bind accepted candidate `f0d2f820f5b3bed541cee64b617f4e23fe6b0342d1b2db1541368df31c573512` and inventory SHA-256 `5e7132ce05a5dc2a569826444ea15f67e9893c725c9ec24c884872bd93917f7c`. Freeze exact target hashes and static AST/text observations. No continued path discovery. This evidence supports only explicit no-current-source disposition, never business status or broader authority; never apply to no-access records.
- Manga retains `no_current_goal_access`, zero probes and `BLOCKED_BY_AUTHORITY` until owner decision. Agent cannot create exceptions or remove difficult projects.
- Light Novel historical governance completion is not current business phase. Scheduler tick/pause facts describe scheduler operations only; business state without a source stays unknown.
- Desktop Magnet README per-item output statuses are not project progress. Unknown status/next fields retain reason and evidence; path existence/title alone is insufficient.
- Relations remain proposals with evidence. Do not confirm program links, visual families, implementation scope or user choice.
- No UI implementation, Figma, external writes, Git, publication or remote effects. Figma/Manga dependencies remain separate. Source-layer completion never becomes `CONNECTED_AND_VERIFIED` without later UI PASS.
- Root owns STATE, protocols, manifest/adapter metadata, evidence and control plane. Repair owns only explicitly assigned disjoint source/test files; no children, Git or external effects. Preserve unrelated dirty work, storage state and TC2 accepted design infrastructure.

## Acceptance

1. New manifest revision binds current registry hash and all 24 IDs, with no duplicates/omissions and preserved older versions.
2. Every project has a machine-validated source disposition: `SOURCE_RESOLVED`, `EXPLICIT_NO_CURRENT_SOURCE_VERIFIED`, or genuine `BLOCKED_BY_AUTHORITY`. Intermediate parsing/unavailability failures remain explicit and cannot masquerade as these results.
3. Read minimum permitted entry content for the three projects without current-state declarations; retain static evidence. Do not infer from titles or paths.
4. Safely derive Light Novel exact control-state paths from allowed static rule/script evidence; never run probes. Model scheduler facts separately from unknown business phase.
5. Derived source declarations bind registry project, allowed evidence entry, evidence hash, exact path literal/declaration, format, field meaning and permission basis. Validation rejects escape, sensitivity or stale evidence.
6. Persist immutable refresh events with request ID, manifest/adapter identity, source fingerprints, start/end times, per-project result/errors. Current view remains a rebuildable Hub projection, not external business authority.
7. Success updates current snapshot/last-success; later failure retains that success explicitly stale with failure time/reason. No success means unavailable/unknown.
8. Bounded whole-manifest refresh commits per-project results. One failure/offline disk/corruption never deletes other results. Same request is idempotent, concurrent conflicts explicit.
9. Restart and deletion of derived views rebuild the same current/history result. Unknown schemas, broken refs, registry/adapter drift or contradictory source hashes fail closed.
10. Relation facts carry project IDs, kind, shared task, differences, non-shared aspects, evidence refs, time and `proposed` status. No evidence means unknown; pipeline relation cannot imply shared visual family or formal link.
11. Every non-Manga project reaches a source-layer terminal disposition with real program evidence; Manga remains counted and authority-blocked. Connection evidence remains below final UI-verified acceptance.
12. Root records real dependencies and exactly one next action only in STATE; preserve storage facts and bootstrap at most 8192 bytes.

## Verification, evidence and recovery

Run runner/gate before writes. Verify coverage, derived source boundaries/no execution, persistence/restart, partial failure, last-success retention, CAS/idempotency/concurrency, schema drift and relation evidence; run repository, state consistency and diff checks. Register preimages, task-owned diff, manifest/adapter hashes, per-project results, commands/exit codes, rebuild proof and bootstrap size. Freeze exact candidate for fresh Judge, then Governor decision. Recovery uses only task-owned preimages/inverse changes; no reset, stash or overwrite of unrelated work.
