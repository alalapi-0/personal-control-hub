# ALL-PROJECTS-CODEX-GOVERNANCE-V1-BOOTSTRAP-v1

Frozen by read-only Governor `/root/startup_governor` in native task `01a07ef7-735b-7270-9c21-3347e1232b5a`; CREATE_TASK decision is in the native transcript. Owner activation and `docs/all_projects_governance_execution.md` supply authority. Lane: GOVERNED. Root is the only writer.

## Objective

Create a Codex-only startup, one independent STATE management entry, scoped checks, exhaustive coverage and manga identity evidence, main delivery-condition inventory, and a minimal existing-Hub connection mapping proposal. Deliver the accepted startup unit to verified Hub main. Manga repair, all-project connection implementation, global configuration changes and Feishu connection are later candidates.

## Boundaries and effects

- No Cursor configuration/files/processes/sessions/hooks/rules/MCP reads, stats, enumeration or effects, including `.cursor/**` and `prompts/cursor_project_driver.md`.
- Preserve the original dirty worktree and its current_work text/semantics. Candidate target main has no STATE/current_work; do not import the other worktree's unrelated task. Do not merge its 21 old commits/590 files.
- Root writes only this Hub startup unit in an isolated worktree from verified main `15a4971061b60b43255fa709a48aaae08bb64b45`. No external project mutation, manga business/asset change, global Codex configuration change, credentials/account/model/fees, production or storage changes.
- Default scan and action protections stay; task selection grants no authority. Existing owner authorization applies only to this activated task. No hard failure suppression or relabeling.
- Root may fetch, create an isolated worktree, commit the accepted task unit and normally push/merge verified Hub main. Recheck default branch, protection and remote head before delivery. No force push, remote changes, protection bypass, unrelated history, or external project pushes.
- Registry changes and old connection-module transplantation are not required in this startup. Record discrepancies and future work explicitly.

## Bootstrap order

Register exact preimages and ownership. Because the legacy check accesses the excluded host and writes logs/Git, Root may first repair only its scoped Codex path and required tests. Instrument the runner and subprocesses against excluded-host access; immediately run the scoped check. Bootstrap creation of the missing canonical STATE and minimal router is necessary for that check. After it passes, complete management and evidence, freeze the candidate, obtain a fresh read-only Judge, then Governor decision before delivery. Unknown task identifiers fail closed.

## Acceptance

1. STATE has one independent task_id/status/unit/contract/evidence/delivery/budget/next_action entry; current_work is unchanged relative to target preimage; original dirty checkout is unchanged.
2. Explicit scoped runner and subprocesses do not read/stat/glob/open excluded-host paths. Negative instrumentation includes environment, core checks and secret scans.
3. Environment, real secret, policy, canonical-state, 8192-byte startup and round consistency failures remain effective; nonzero child exits cannot become PASS. Unknown scope fails.
4. Scoped runner, targeted tests, repository/consistency/registry validation, YAML/JSON parsing and startup size pass.
5. Coverage reconciles registry, all native saved projects and one-level known workspace entries. Every candidate has stable identity, canonical root/alias/provenance, enabled/excluded/offline/no-Git/state-source disposition. Unresolved discoveries are not silently removed.
6. Manga identity is distinguished from anime factory via native task, registry, real Git root and remote. Uncertain legacy manga path stays unresolved, not guessed.
7. Each coverage item records Git/no-Git, root, remote, branch/upstream, observable protection/PR/CI, dirty ownership uncertainty and delivery feasibility. Unknown stays explicit.
8. Proposal maps all seven execution-document field groups to existing Hub registry/adapters/source/record/refresh/service concepts without another business-state authority or module transplant requirement.
9. Exact diff contains only startup-owned changes, no Cursor/unrelated dirty/credential/UI/Figma/asset/external-project content.
10. Fresh Judge PASS and Governor acceptance precede delivery. A receipt proves candidate ancestry on observed remote main; a PR or side branch alone remains pending_delivery.

## Evidence and recovery

Baseline records preimages and protected original hashes. Candidate identity covers files and STATE semantics; only review/delivery/management telemetry metadata is excluded. Judge, Governor and delivery metadata do not hash themselves. Root reproduces checks and preserves failure fingerprints. Forward repair only; no reset/clean of the original checkout. Verify uncertain remote writes before retry. Native cumulative Goal telemetry covers parent and child work; no explicit token budget was requested, cost is unknown. No new scheduler or quota ledger.
