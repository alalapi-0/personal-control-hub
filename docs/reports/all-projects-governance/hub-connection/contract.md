# Hub unified connection contract

`ALL-PROJECTS-CODEX-GOVERNANCE-V1-HUB-CONNECTION-v1` — GOVERNED.
Governor `/root/startup_governor` restated the existing contract under the same
native Goal. No new task, lineage, attempt or budget is created. Root is sole writer.

## Frozen objective and boundaries

Deliver one Hub-only shared declaration/output schema, lightweight project
template, typed source reader, validator, resumable refresh and common JSON,
Markdown and disabled Feishu preview. Reuse necessary original connection,
record, refresh and project-service behavior; do not merge its branch or import
unrelated UI/Figma history. Preserve actual consumers through coherent adaptation.
Implementation paths and module boundaries remain adaptive.

Exactly 26 registry records remain visible: 24 local identities and two removed
local identities. The 17 owner-excluded cloud discoveries stay outside this output
and execution denominator. External-project declaration rollout is a later unit.

Only this isolated Hub worktree may be written. The original Hub is a read-only
source: preserve its dirty work, current_work, UI work, global drafts and unrelated
files. No external-project, Manga, global Codex, Cursor, production, storage,
credential, account, model, paid-service or real Feishu effects. Build from latest
verified Hub main; never reset/clean/overwrite/commit the original checkout.

Registry owns machine roots. Project declarations contain stable identity,
relative source pointers, format, typed mapping and validation entry; business
facts derive only from the project sole source. No project-code execution,
unauthorized alias/root traversal or sensitive path access. Removed entries
remain disabled and unopened even if flags are changed. No secret values enter
source, fixtures, previews, logs, evidence or Git.

Do not create a second business authority, manually editable Hub project-progress
copy, scheduler, watcher, lock service, Goal mechanism or duplicate ledger.
A rebuildable Hub-local refresh ledger/cache with source fingerprints is allowed.
Feishu stays disabled, without outbound calls or write-back; only approved
FEISHU_APP_ID, FEISHU_APP_SECRET and FEISHU_WEBHOOK_URL names are declared.

## Acceptance

1. Latest main base, original dirty preimages and the delivered Manga prerequisite
   are registered. Reused behavior has source/destination identity, dependency
   reason and current test coverage; prior UI/Figma acceptance is not evidence.
2. Versioned declaration/output schemas, valid template, instructions and callable
   validators are delivered. Invalid identity, absolute/traversing/sensitive paths,
   formats, typed mappings, aliases and unknown fields fail closed.
3. Seven semantic groups are traceable: identity/source; current work; progress;
   blockers/recovery; validation/delivery; freshness; per-field provenance and
   exhaustive output coverage. Every business value has source evidence or an
   explicit unknown/error reason. completed, accepted and delivered stay distinct;
   no invented percentages or promotion from unknown/stale states.
4. JSON and Markdown have exactly 26 unique IDs. Every row retains a read
   disposition, including missing declaration/source, offline, invalid, denied,
   stale, authority drift and removed. Removed rows use registry facts only and
   cause zero filesystem access despite attempted activation flags.
5. Refresh proves one source changes only one row, unchanged refreshes are
   idempotent, interruption resumes after restart, fingerprint drift invalidates
   or rebinds correctly, and newer failure remains visible over stale prior success.
   Cover corrupt/missing/offline/permission/root escape/alias and authority drift.
6. A bounded real read-only smoke produces all 26 rows without writing any project.
   Existing shared consumers compile and pass relevant behavior tests.
7. JSON, Markdown and Feishu previews consume the same normalized records and
   preserve IDs, fingerprints, freshness/errors, blockers, next actions and separate
   lifecycle facts. Deterministic update identities and instrumented zero network,
   credential read, message send, space creation and external effects are proven.
8. A fresh reader uses only Hub documentation/output to reconcile all 26 records
   and find each source or explicit failure reason, without visiting project roots.
9. Scoped runner passes before first write and at closure. Repository,
   round-consistency, registry, structured-format, schema, source, record, refresh,
   restart, projection, consumer, preview, secret and diff checks pass.
10. STATE updates only all_projects_governance, preserving other fields/current_work,
    bootstrap/Manga receipts and one truthful next action. Register exact semantic
    candidate, preservation/provenance, previews and reproducible checks. A fresh
    Judge PASS and Governor approval precede normal Hub-main delivery and remote
    ancestry verification. PR/local/side-branch alone is pending_delivery.

## Evidence, recovery and cumulative budget

The baseline registers source preimages, main, ownership, prerequisite and actual
native telemetry. Register source-to-destination provenance, schema identities,
26-row reconciliation, field traces, fixtures/real smoke, zero-effect tests,
preview identities, checks/exits, semantic hash, independent review and remote
receipt. No self-containing hash; decision/delivery metadata remains outside the
reviewed semantic identity. Forward repair only; restore only exact owned preimages
after drift checks. All work retains the same cumulative native Goal budget;
no explicit token cap or cost telemetry exists. Reserve validation/review/delivery
and closure capacity. Do not treat partial rollout or real image redo as complete.
