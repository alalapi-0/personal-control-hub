# Hub normal entry integration contract

`ALL-PROJECTS-CODEX-GOVERNANCE-V1-HUB-ENTRY-INTEGRATION-v1` — GOVERNED.
Established read-only by Governor `/root/hub_connection_governor`; activated by Root after light-novel receipt `698e075df35f81d8e0252f9c305d9297d13e9735` reached main and CI34206344158 succeeded. Existing owner Goal authority and cumulative budget apply, without reset.

## Objective and ownership

Make the normal Personal Control Hub entry use accepted Hub2.0 authority through existing service, CLI, HTTP and UI consumers. Cover26 stable local inventory records:24 local and2 removed;17 cloud excluded. At this unit's source inventory, light-novel resolves and the other23 missing declarations remain explicit failures. Preserve all21 typed business fields, full source_record, latest attempt, separate last success, freshness and removal disposition. Do not claim remaining project rollout complete.

Root owns control planes, state, evidence, Git, installation and live Hub ledger. Repair may own only explicitly assigned facade and regression code. No descendants. Root selected isolated Hub repository; all external projects and original Hub remain read-only in Stage A. Existing application/schema/resolver/ledger/preview evidence may be reused only for unchanged exact hashes.

## Boundaries

Protect original Hub HEAD0ca1ee547990fd876e9917c8c6ee1fa7d2bb8142, index, status and27 registered hashes, except separately registered task-owned field-level deltas in Stage B. Preserve its21 unrelated commits, approximately590 unrelated paths, dirty UI/global/control-plane work, current_work, storage and design metadata. Never import the whole branch. Preserve original HTTP/UI history; a Goal-required consumer repair must first be delivered on main. Do not claim that original-only UI history was delivered.

Old design ledger and Hub2 ledger remain separate: no migration, fallback, mixed reads or writes. External projects are read-only; explicit refresh may read only declarations and their authorized state source. Removed and excluded cloud roots receive zero reads. No project execution/writes, Cursor resources, global configuration, credentials/.env, paid API, real Feishu, publication, deployment, force/protection bypass, reset/clean/stash/history rewrite. Original Hub receives no commit or branch update.

## Serial stages

A. From verified main containing20eff90cf46eefcf127ba5f19c9dbb397d4e770b, implement the minimum ProjectService list_projects/get_project/refresh service contract and actionable errors. Delegate business authority to Hub2 resolver/ledger/project_snapshot; no legacy bundle or reconstructed manager. Preserve existing original consumer capabilities without importing unrelated history. Register exact candidate, Root checks, fresh Judge and Governor; normal main push with ancestry and CI.

B. Only after A delivery/CI, Root installs accepted files by registered preimage CAS and targeted patches. Bootstrap/control-plane merges are field-level: task-only AGENTS/STATE entry, per-ID26 registry with storage/UI metadata retained, task-scoped gate/policy, strict storage checks except2 removed records and the exact existing Goal manga exception. Preserve current_work and unrelated state. Every reusable Goal change must already exist on main. Original-specific merge recipe/receipt is separately reviewed and delivered to main.

Then one bounded real Hub2 refresh may write only the new Hub ledger/cache. Generate local JSON/Markdown/disabled-Feishu previews and run normal original entry. A fresh Hub-only reader checks inventory and read boundaries; fresh Judge and Governor accept the exact installation/runtime evidence. Original effects remain confined to registered task paths and new Hub2 ledger/cache.

## Acceptance

1. Service/CLI/HTTP/UI expose26 stable IDs,24 local2removed;17cloud absent and never resolved.
2. Actual refresh:1 valid light-novel,23 explicit missing_declaration,2 removed; no legacy/default success.
3. Normal server imports and list/detail/query/refresh/error routes work through facade.
4. DTO retains complete source_record,21 typed fields, hashes, latest failure, separate history, freshness, unknown and removed state.
5. Hub2-only business authority; no legacy manager/bundle/design ledger access.
6. List/detail do not read project roots or create ledger; explicit refresh bounded; removed/cloud zero reads.
7. CAS/idempotency/stale rejection/partial failure/restart/corruption/latest failure semantics remain effective.
8. Current failures stay visible in CLI/HTTP/disabled-Feishu/UI; old success never conceals failure.
9. JSON/Markdown/Feishu full normalized parity across all26, not ID-only or summary-only checks.
10. Minimal real original-browser proof shows26 and correct valid/missing/removed states with no material console/network error; local compatibility only.
11. Registry per-ID storage/UI fields and ordering semantics remain; strict storage check has only exact evidenced exceptions.
12. Original HEAD/index/status/27 protected hashes preserve unrelated contents; exact task field deltas separately registered; no unrelated paths changed.
13. Existing service/integration/CLI/UI test intent remains with Hub2 fixtures; old bundle-specific assertions may change, no silent lost checks.
14. StageA implementation and StageB receipt reach remote main with applicable CI; no original Git delivery.
15. Exact candidate/preimages/modes/source commits/commands/exits/counts/parity/UI/read-write traces/preservation are registered before fresh reviews.

## Evidence and recovery

Semantic content identity excludes only precisely registered decision/delivery metadata, never ordinary blockers or self hashes. Same unchanged candidate is not re-reviewed. Forward repair; restore only exact owned registered preimages after drift check. A task-created Hub2 ledger may be rebuilt/removed only through exact compare-and-set recovery; old design ledger stays untouched. No broader recovery or completion claims. StageA approval does not substitute for StageB acceptance.
