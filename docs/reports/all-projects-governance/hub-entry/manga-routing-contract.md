# Manga state routing through Hub

`ALL-PROJECTS-CODEX-GOVERNANCE-V1-HUB-MANGA-ROUTING-v1` — GOVERNED. Read-only Governor `/root/hub_connection_governor` established this contract and its implementation clarifications before mutation. Root activates it after Manga main `1aa3fb8a80241bf7a533b13eeb08771a428f4839` passed full audit and CI34226749921. Existing Goal, cumulative budget and failure history continue without reset.

## Objective and boundaries

Connect Manga's installed `hub.connection.yaml` to its sole `.agent/STATE.yaml` through the existing Hub2 entry. Initial source hashes are respectively `a6f6baea6521056ef1b2fc52b9731f68a9a646de395fb5573f6a5743b5ba3f69` and `1d400617e6189437fc5886419dca776f6a5c0a11b7290318fe8798ae71a01dd7`.

Root alone owns control planes, state, evidence, Git and installation. Stage A owns the isolated Hub source from verified main `deb2bc12cc6d0fc78ae7723db32b0961d3e4711c`; original Hub is read-only until Stage B. External projects are read-only throughout. Manga reads are limited to its declaration and YAML state; no old Markdown state, database, media, credentials, Cursor, provider, worker, real API or validation_entry execution. No real Feishu, project execution, storage changes, old-ledger mixing or general service/UI/schema redesign.

Change only Manga's registry state path and the exact named-route validator from `.agent/STATE.md` to `.agent/STATE.yaml`, with necessary existing regression fixtures. Keep all other permission, storage/UI and legacy-exception fields and the other25 registry records unchanged. Do not allow both paths, fallback or arbitrary paths. Keep removed roots unread and17 cloud projects excluded.

Protect original Hub HEAD `0ca1ee547990fd876e9917c8c6ee1fa7d2bb8142`, branch, index, current_work, registered files, unrelated dirty contents and unmerged history. Stage B uses exact preimage CAS and reversible per-file/hunk installation. No original Git commit, whole-branch import, reset, clean, stash, force push or history rewrite. Original-only HTTP/UI history is not imported for this routing unit.

## Acceptance and serial delivery

1. Registry diff is exactly the Manga sole-state pointer; the named-route validator accepts YAML and rejects the old Markdown route. All other storage and permission checks remain effective.
2. Existing declaration/schema, registry, repository/state and relevant regression checks pass. Explicit Manga business-read permission remains effective despite enabled=false; historical exception grants no authority.
3. Registry bytes change the shared authority hash. Execute one explicit full26 refresh with a new request ID and expected ledger head. Record expected authority/read-time/result-reference changes; other25 business/source/disposition facts must remain evidence-consistent. Two removed projects receive zero root reads. Preserve existing history and failures.
4. Request replay performs zero source reads and zero appends; restart/rebuild retains identical ledger facts. Current/list/detail use only Hub authority and ledger, with no project-root reads.
5. Coverage is26=2 resolved,22 missing_declaration,2 removed_local;17 cloud absent. Nonzero refresh exit for22 missing declarations is retained, not converted to success.
6. Manga preserves20 known fields,1 honest unknown,140/199 with its observed counting basis,3 blockers, completed=false, accepted=false and verified code delivery. Actual image repair and overall project acceptance remain incomplete.
7. JSON, Markdown and disabled Feishu contain identical complete normalized records, including source_record, current failure, history and freshness. Preserve all21 typed fields.
8. Stage A validates source's existing CLI, ProjectService, ledger, projections and pure view helpers. Register the exact candidate, preimages and Root checks; fresh Judge reviews it and Governor decides. Only then normal Hub main push and remote ancestry/CI verification.
9. After Stage A main/CI succeeds, Stage B installs approved exact hunks into original Hub, performs a real local full26 refresh and checks existing HTTP/UI. Show Manga resolved,22 missing failures visible,2 removed disabled, no unexpected console/network error, and ordinary reads with zero external-root access. A fresh read-only Agent verifies handoff solely from Hub.
10. Register the original installation and preservation evidence; fresh Judge and Governor decide. Deliver only the concise receipt and necessary management metadata via Hub source main, then verify remote and CI. Original Hub Git state remains unchanged.

## Evidence identity and continuation

Use `manga-routing.json` in this existing evidence directory for this bounded unit; do not add a new ledger or report system. Freeze actual content and exact preimages. Explicit PASS/decision/delivery/next-action metadata stays outside semantic identity; business facts and route changes do not. Reuse unchanged accepted code/tests, preserve failed evidence and one accurate next action in the existing STATE task entry. This unit does not finish the22 remaining project declarations, original pending-content delivery, global versionized sources or actual Manga redo.
