# 项目终极目标（兼容说明）

长期方向的当前权威已迁移到根目录 `NORTH_STAR.md`。本文件只为旧链接保留，不再单独维护另一份终极目标。

读取触发：旧报告或旧链接引用本路径时。更新触发：仅当兼容指针失效。

一句话目标：把所有个人项目纳入本地优先、可审计、可恢复、低上下文成本的项目注册与治理系统；中央仓库保存索引、状态、协议与证据，不复制业务项目内容。飞书是未来交互入口，不是当前事实源。
