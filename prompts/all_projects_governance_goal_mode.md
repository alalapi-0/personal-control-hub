# 项目数据发布与同步 v3 · 共用目标执行 Prompt

准备产物，保存或阅读不启动实施。读者：所有者及后续Codex/Cursor执行者；路线或授权改变时更新。

两种入口使用完全相同的正文与STATE：Codex通过已有Goal入口提交；Cursor通过其当前可用的目标执行入口提交。不要假定两端支持相同斜杠命令，不安装/操控另一个编辑器，也不创建替代Goal调度器。正文首句就是显式v3启动指令。

~~~text
开始按 personal-control-hub 的 docs/all_projects_governance_execution.md v3 和 data/roadmap/project_data_v3.yaml 持续实施项目数据发布与同步目标。先读AGENTS.md、STATE.yaml及该计划的当前阶段/项目条目。沿用all_projects_governance唯一状态、已有成果、失败/恢复点与累计用量；准备完成不等于实施完成，不重开无关任务。

目标：每个适用项目通过普通脚本/函数按统一声明和Schema导出标准快照；Hub启动及手动操作自动同步，只读取这些快照，补充共享GitHub等API数据并生成可查询数值及变化历史。共用Git/导出/验证逻辑，领域只写必要映射；不重复实现公共API，不让Hub直接耦合各项目业务数据库。关闭Agent、模型和MCP后，统计、同步、校验、查询仍须运行。模型最后按需分析有界摘要。

严格按路线修复审核回流：一审/二审是实际审核阶段，失败绝不自动升级二审。一审不通过保留该版本意见，进入待重做→重做中→新版本待提交一审；只有新候选持久化且显式送审后才进入新版本待一审。candidate_revision、rework_count、submission_attempt与review_stage分开。一审通过且项目配置要求后才进入真正二审。自动验证、审核、验收和交付分别绑定版本；重复/迟到事件不覆盖新版本或重复入队。导出刷新不触发重做，改名不算修复；真实失败案例验证完整重做流程及重试/恢复。

实施前核对当前计划与旧v2执行记录，在同一STATE条目建立v3游标并保留历史；V3-01完成基线和验收项绑定，再按阶段依赖选最小可验收单元。V3-08逐项目推进，采用路线中的全登记映射，不能一批只验一个项目就标整批完成。每轮复用未变证据，只读取当前单元所需内容；遇局部外部阻塞继续独立单元，最后保留真实缺口。

统一身份、管理/里程碑、业务数值、问题、验证、交付、数据质量及领域扩展。每个映射都能追踪项目、源、选择器/函数、指标、单位、维度、时间窗口、计数口径、缺失原因与版本。目标/优先级/人工决定来自明确记录，不从提交量猜项目进度；0、未知、不适用和失败分开，完成、验收、交付分开。

本阶段不生成图表、不做UI改造；只接通现有启动/同步入口并准备展示配置。飞书只保留禁用本地映射。云项目排除，removed_local禁止探测/读写/重试；空项目、无Git、缺源保留分母和处置。不操作编辑器/账号运行时、全局模型/权限，独立项目按下段整目录迁移、不自动建远端、不调用未授权付费生成。漫画实际生成缺授权时保留未完成，不用mock或状态改写冒充闭环。

按我的要求，将除Personal Control Hub外的独立本地项目迁到外置盘，新迁项目默认/Volumes/AI_WORK_SSD/Projects。已在外盘不重搬，Hub仓库内部模块随Hub留本地，removed_local不碰。整目录迁移并保留Git及未提交独有内容；由程序做必要完整性/Git/入口核对，不逐文件让模型检查。验证目标后切换路径并完成原目录清理，必要时建立旧路径连接，不重复保留完整副本。复用现有存储治理唯一状态和规则入口，不重开无关全盘清理；盘未挂载/目标冲突/校验失败保留原数据。只提交实际内容和路径配置变化，不为移动本身造空Git提交。

严禁浪费token：程序做统计、解析、去重和检查；不注入全仓/全历史/大报告，不让子Agent继承完整历史，不重复父子调查；未变不重复全测/重审，保留长任务句柄，不短间隔轮询。同方法同失败两次无进展就诊断或换方法，不重复额度拒绝；不改模型/额度绕过。普通工作不强制三角色/合同/收据，不新增逐轮说明。机器数据保留必要维度，给模型的默认摘要≤8KiB且保留覆盖分母/错误计数，明细分页。

我授权在本路线必要范围内修改Hub和相关项目的声明、导出器、同步/数据代码、测试与规则，清理被替代且可恢复的无用材料，并按归属处理原有未提交工作。每个通过相关验收的单元正常提交、合入、推送对应GitHub真实主分支并核对远端包含关系；不得只推Hub代替子仓、强推、绕过检查或分支保护。保护其他任务、独有原始数据与凭据；本地无仓库/远端保留缺口。

每轮收尾只更新现有STATE和必要证据引用：当前阶段/项目/单元、验收结果和版本、阻塞、交付、唯一下一步。沿用已有轮末进度核算，区分执行增量、范围变化和证据修正；累计逻辑轮去重，未知历史不造数字，剩余轮数给区间及置信度。阶段数量不等于累计轮数。工具调用、审查和状态问答不另计轮。持续推进至全部验收成立；不能以计划、模板、报告数量或部分项目成功宣称全目标完成。
~~~
