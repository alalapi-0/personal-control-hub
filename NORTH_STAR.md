# North Star

## 文档职责

- 读者：所有者，以及处理重大产品、架构、数据边界或集成决策的 Agent。
- 目的：定义 personal-control-hub 的长期方向、成功标准和不变量；不记录当前进度或逐轮日志。
- 更新触发：长期目标改变、系统边界改变、权威数据模型改变、采用新的主交互入口或外部集成成为正式能力。
- 非触发：普通子项目推进、一次扫描、一次治理轮或实现细节调整。

## 北极星

把分散在本机与外部存储中的全部个人项目，组织成一个本地优先、可审计、可恢复、低上下文成本的个人项目管理系统：任何时刻都能知道有哪些项目、每个项目的权威入口与当前状态、唯一下一步、风险边界、最近证据和版本历史，同时不把所有项目内容复制到中央仓库。

## 系统角色

personal-control-hub 是：

- 项目注册表：维护稳定 ID、便携路径、项目类型、状态和权威文件指针。
- 控制面：路由 Agent 到最小必要上下文，维护单一当前状态与治理规则。
- 证据与版本入口：把 accepted 里程碑和治理轮固化为小而清晰的 Git 版本。
- 计划与复盘层：连接项目、现实目标、下一步和周期性复盘。
- 集成边界：未来向飞书等交互界面提供经过筛选的状态与待确认事项。

它不是业务仓库 monorepo、所有项目的内容镜像、秘密存储、全量扫描器或自动获得高风险权限的 Agent 平台。

## 设计不变量

1. 本地文件和 Git 是首要可恢复基础；不依赖单一云服务。
2. 每类事实只有一个当前权威；投影、报告和历史必须明确非权威。
3. 默认上下文只包含路由器与当前状态，其他材料按任务加载。
4. 中央仓库只保存索引、协议、状态和证据，不复制外部项目正文或大型资产。
5. 外部项目默认只读；任何写入、删除、发布、登录或付费动作服从当前权限与审查。
6. 每个 accepted 里程碑/治理轮形成一个可回退、可追踪的 Git 交付单元。
7. 自动化输出是 proposal；所有者保留优先级、战略和不可逆动作的最终决定权。

## 飞书方向

飞书可以在未来成为移动入口、通知面板、复盘视图和确认队列，但不是当前事实源，也不能成为唯一数据库。正式接入前必须明确：数据最小化、同步方向、字段映射、身份与权限、失败回退、审计日志和秘密存储。当前阶段只保留适配边界，不调用真实 API、不发送消息、不写入真实空间。

## 成功标准

- 新项目能在几分钟内登记，并能定位其唯一规范、状态和下一步。
- 日常推进无需加载整套历史；默认启动包稳定小于 8KB。
- 子项目完成里程碑后，注册表/状态与 Git 证据同步且不产生多份竞争真相。
- 存储治理、创作、自动化和未来项目都可通过同一套轻量路由纳入管理。
- 即使飞书或其他服务不可用，本地系统仍完整可用、可审计、可恢复。
